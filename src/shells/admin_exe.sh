#
#opciones:
#para hacer un iso de los archivos en el disco dura usamos:
# $ mkisofs -r -o [nombre del archivo ISO] [directorio contiendo los archivos]
# para quemar el iso en el cdrom grabable
# cdrecord -v speed=8 dev=0,0,0 -data [path completo al directorio iso /ruta/archivo.iso]
